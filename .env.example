# ================================
# Matcha Trading Platform
# Environment Configuration
# ================================

# ============ Database ============
# PostgreSQL connection for Docker Compose
POSTGRES_USER=matcha
POSTGRES_PASSWORD=matcha_secret
POSTGRES_DB=matcha_db
POSTGRES_PORT=5432

# Prisma database URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://matcha:matcha_secret@localhost:5432/matcha_db

# ============ API Server ============
# Port for the NestJS API server
PORT=3001

# API base URL (used for Swagger documentation)
API_URL=http://localhost:3001

# Node environment
NODE_ENV=development

# ============ Authentication ============
# JWT secret key - MUST be changed in production!
# Generate with: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# JWT expiration time (e.g., 15m, 1h, 1d)
JWT_EXPIRES_IN=1h

# Refresh token secret
JWT_REFRESH_SECRET=your-refresh-secret-change-in-production

# Refresh token expiration
JWT_REFRESH_EXPIRES_IN=7d

# ============ CORS ============
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# ============ Rate Limiting ============
# Short-term rate limit (per minute)
THROTTLE_TTL=60000
THROTTLE_LIMIT=100

# Long-term rate limit (per hour)
THROTTLE_TTL_LONG=3600000
THROTTLE_LIMIT_LONG=1000

# ============ Frontend ============
# Next.js public API URL
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# ============ Optional Services ============
# Redis (for caching/sessions - optional)
# REDIS_URL=redis://localhost:6379

# Email service (optional)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password
# EMAIL_FROM=noreply@matchatrade.io

# File storage (optional)
# AWS_ACCESS_KEY_ID=your-aws-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret
# AWS_REGION=ap-northeast-1
# AWS_S3_BUCKET=matcha-uploads
